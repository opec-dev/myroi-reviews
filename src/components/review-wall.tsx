"use client";
import Link from "next/link";
import { useQuery } from "convex/react";
import { api } from "../../convex/_generated/api";
export function ReviewWall({slug,preview}:{slug:string;preview:boolean}){const data=useQuery(api.reviews.publicBySlug,{slug});if(data===undefined)return <main className="review-wall-page"><div className="empty-state">Loading reviews…</div></main>;if(!data)return <main className="review-wall-page"><div className="empty-state">This review wall is not published.</div></main>;return <main className="review-wall-page">{preview&&<Link className="preview-back" href={`/client/${slug}#reviews`}>← Back to client dashboard</Link>}<header className="review-wall-header"><div className="logo-lockup"><img src={data.business.logoUrl??"/brands/yorkshire-roofing.png"} alt={`${data.business.name} logo`}/></div><div><span className="stars">★★★★★</span><strong>Trusted by our customers</strong></div></header><section className="review-wall">{data.reviews.map(review=><article className="wall-review" key={review._id}><div className="review-meta"><span className="stars">{"★".repeat(review.rating)}</span><span>{review.sourceLabel}</span></div><blockquote>“{review.excerpt}”</blockquote><div><span className="reviewer-avatar">{review.reviewerName.slice(0,1)}</span><strong>{review.reviewerName}</strong></div>{review.sourceUrl&&<a href={review.sourceUrl} target="_blank" rel="noopener noreferrer">View original review ↗</a>}</article>)}{!data.reviews.length&&<div className="empty-state">No published reviews yet.</div>}</section></main>}
