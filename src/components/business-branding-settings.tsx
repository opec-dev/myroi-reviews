"use client";

import { ChangeEvent, useEffect, useState } from "react";
import { businessBrandingStorageKey, defaultBusinessBranding } from "@/lib/demo-data";

export function BusinessBrandingSettings(){const [branding,setBranding]=useState(defaultBusinessBranding);const [saved,setSaved]=useState(false);useEffect(()=>{const value=localStorage.getItem(businessBrandingStorageKey);if(value)try{setBranding({...defaultBusinessBranding,...JSON.parse(value)})}catch{}},[]);function upload(key:"logoUrl"|"iconUrl",event:ChangeEvent<HTMLInputElement>){const file=event.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{setBranding(current=>({...current,[key]:String(reader.result)}));setSaved(false)};reader.readAsDataURL(file)}function save(){localStorage.setItem(businessBrandingStorageKey,JSON.stringify(branding));setSaved(true);window.dispatchEvent(new Event("myroi-branding-updated"))}return <><div className="section-heading"><div><span className="eyebrow">Client identity</span><h2>Logo, QR icon & colors</h2></div></div><p className="panel-intro">Use the full logo in the funnel and card. Upload a separate square icon for the center of the QR code and compact spaces.</p><div className="branding-upload-row"><label className="brand-upload"><strong>Main business logo</strong><img src={branding.logoUrl} alt="Main logo preview"/><input type="file" accept="image/png,image/jpeg,image/webp,image/svg+xml" onChange={event=>upload("logoUrl",event)}/><small>Wide or horizontal logos work well here.</small></label><label className="brand-upload square"><strong>Square business icon</strong><img src={branding.iconUrl} alt="Square icon preview"/><input type="file" accept="image/png,image/jpeg,image/webp,image/svg+xml" onChange={event=>upload("iconUrl",event)}/><small>Used as large as safely possible inside high-correction QR codes.</small></label></div><div className="settings-grid"><label>Primary color<input type="color" value={branding.primaryColor} onChange={event=>{setBranding({...branding,primaryColor:event.target.value});setSaved(false)}}/></label><label>Secondary color<input type="color" value={branding.secondaryColor} onChange={event=>{setBranding({...branding,secondaryColor:event.target.value});setSaved(false)}}/></label></div><div className="settings-save"><small>{saved?"Saved in this pilot browser.":"The print-card banner and client accents use these colors."}</small><button className="button primary" onClick={save}>Save business branding</button></div></>}
